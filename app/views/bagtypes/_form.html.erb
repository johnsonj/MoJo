<%= form_for(@bagtype) do |f| %>
  <% if @bagtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bagtype.errors.count, "error") %> prohibited this bagtype from being saved:</h2>

      <ul>
      <% @bagtype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :size %><br />
    <%= f.number_field :size %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

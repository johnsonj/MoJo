<%= form_for(@item_description) do |f| %>
  <% if @item_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_description.errors.count, "error") %> prohibited this item_description from being saved:</h2>

      <ul>
      <% @item_description.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :rarity %><br />
    <%= f.text_field :rarity %>
  </div>
  <div class="field">
    <%= f.label :image_id %><br />
    <%= f.number_field :image_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

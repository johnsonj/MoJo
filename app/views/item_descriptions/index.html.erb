<h1>Listing item_descriptions</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Rarity</th>
    <th>Image</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @item_descriptions.each do |item_description| %>
  <tr>
    <td><%= item_description.name %></td>
    <td><%= item_description.description %></td>
    <td><%= item_description.rarity %></td>
    <td><%= item_description.image_id %></td>
    <td><%= link_to 'Show', item_description %></td>
    <td><%= link_to 'Edit', edit_item_description_path(item_description) %></td>
    <td><%= link_to 'Destroy', item_description, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item description', new_item_description_path %>

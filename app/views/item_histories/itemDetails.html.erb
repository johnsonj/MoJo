<h1>Listing the Item History for: <%= @item.name %></h1>

<table>
  <tr>
    <th>User</th>
    <th>Item</th>
    <th>Latitude</th>
    <th>Longitude</th>
    <th>Signature</th>
    <th>Stamp</th>
    <th>Runningdistance</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @item_histories.each do |item_history| %>
  <tr>
    <td><%= item_history.user_id %></td>
    <td><%= item_history.item_id %></td>
    <td><%= item_history.latitude %></td>
    <td><%= item_history.longitude %></td>
    <td><%= item_history.signature %></td>
    <td><%= item_history.stamp %></td>
    <td><%= item_history.runningdistance %></td>
    <td><%= link_to 'Show', item_history %></td>
    <td><%= link_to 'Edit', edit_item_history_path(item_history) %></td>
    <td><%= link_to 'Destroy', item_history, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item history', new_item_history_path %>

<%= form_for(@bug) do |f| %>
  <% if @bug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug.errors.count, "error") %> prohibited this bug from being saved:</h2>

      <ul>
      <% @bug.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :active %><br />
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label :infectionprob %><br />
    <%= f.text_field :infectionprob %>
  </div>
  <div class="field">
    <%= f.label :lifespan %><br />
    <%= f.text_field :lifespan %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
